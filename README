This is a mirror of http://www.vim.org/scripts/script.php?script_id=2383

The winter.vim is a color sheme file (only for GVIM) which uses a light grey background makes the VIM look like the scenes of winter.
